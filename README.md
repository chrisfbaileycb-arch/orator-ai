# ORATOR.AI // THE AUTONOMOUS SOFTWARE FORGE

> **A closed-loop digital manufacturing appliance that renders traditional developer tooling, configuration fatigue, and low-end SaaS wrappers obsolete.**

Powered by continuous 16+ Model Mixture-of-Experts (MoE) rotation, zero-disk in-memory runtime, and the "Velvet Rope" appointment-slot scarcity execution model.

---

## Key Capabilities

1. **Pearlescent Plasma Sanctum (Three.js WebGL + GLSL)**:
   - Dynamic simplex noise vertex displacement, thin-film iridescent Fresnel sheen, mouse spring physics, and audio spectrum core bloom.
2. **15-Question Conversational Inquest**:
   - 15-segment orbital halo progress ring with cyan-to-gold chromatic sparks.
3. **Velvet Rope Scarcity & Gating**:
   - Live HUD capacity telemetry (`FORGE CAPACITY: 92% | ACTIVE BUILDS: 8/10`), intermittent scarcity alerts, and appointment slot booking.
4. **Multi-Model Router (`model_router.py`)**:
   - OmniRoute-style key rotation (`free` mode) and high-reasoning tier (`paid` mode) across OpenAI, Gemini, and OpenRouter for `architect`, `schema`, `backend`, `frontend`, and `review`.
5. **Session Allowance & $99 Gating**:
   - 3 free practice builds per client ID with automatic transition to `$99 payment_required` on the 4th session.
6. **Dynamic Real-Time Mind Map & 22-Point Audit**:
   - Queries 11 indexed repositories with animated photon energy packets, zero-disk in-memory packaging, live sandbox iframe, and instant ZIP downloads.

---

## Repository Structure

```
├── server.py                   # Core Ephemeral HTTP Server & SSE Stream Engine
├── model_router.py             # Multi-Model LLM Router (OpenAI / Gemini / OpenRouter)
├── backend_knowledge.py        # 11 Indexed Repositories & Knowledge Catalog
├── shift_engine.py             # Invariant-Enforced Deterministic State Machine
├── audit_engine.py             # 22-Point Comprehensive Invariant Audit Suite
├── start_orchestrator.sh       # One-Click Launch Script
├── templates/
│   ├── single_session_template.py
│   └── citadel_template.py
└── static/
    ├── index.html              # Aerospace Command Terminal HUD
    ├── css/styles.css          # Deep Abyss Vignette & Luxury Dark Palette
    ├── vendor/three.min.js     # Bundled Three.js r128 WebGL Engine
    └── js/
        ├── three-core.js       # Pearlescent Plasma Core & GLSL Shaders
        ├── audio-synth.js      # Web Audio Synthesizer & 30Hz Sub-Bass Drone
        ├── mind-map.js         # Real-Time Dynamic Graph & Photon Packets
        ├── agent-orchestrator.js# Phase IV Forge Execution Deck & SSE Listener
        ├── chat-core.js        # Conversational Inquest & Velvet Rope Checkout
        └── app.js              # Application Bootstrap & Scarcity Loop
```

---

## Quick Start

```bash
# Clone the repository
git clone <YOUR_REPO_URL>
cd the-orchestrator

# Launch the server
python3 server.py

# Open in browser
http://localhost:8080
```

---

## Environment Variables (Optional)

To enable live multi-model LLM generation:
```bash
export OPENAI_API_KEY="sk-..."
export GEMINI_API_KEY="AIza..."
export OPENROUTER_API_KEY="sk-or-v1-..."
```
*(Without API keys, the appliance runs in deterministic demo mode with full template generation, mind map visualization, 22-point audit, and ZIP download).*
